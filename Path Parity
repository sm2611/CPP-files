#include <bits/stdc++.h>
using namespace std;

int main() {
	int tc;
	cin>>tc;
	while(tc--){
	    long long int n,k;
	    cin>>n>>k;
	    if((n%2==0) || (n%2==1 && k==1))
	        cout<<"YES"<<"\n";
	    else
	        cout<<"NO"<<"\n";
	}
	return 0;
}
